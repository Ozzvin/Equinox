name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: gofmt
        shell: bash
        run: test -z "$(gofmt -l cmd internal)"
      - run: go vet ./...
      - run: go test -count=1 ./...
      - name: build
        run: |
          $v = (Get-Content VERSION).Trim()
          go build -ldflags "-H windowsgui -s -w -X github.com/Ozzvin/equinox/internal/buildinfo.Version=$v" -o Equinox.exe ./cmd/equinox
          go build -ldflags "-s -w -X github.com/Ozzvin/equinox/internal/buildinfo.Version=$v" -o Equinox-server.exe ./cmd/equinox-server
